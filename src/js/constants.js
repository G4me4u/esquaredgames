const DEBUG = true;

// Screen size
const WIDTH  = 8;
const HEIGHT = 8;

const PIXEL_MARGIN = 2;

// Timing
const TPS = 20.0;
const FPS = 60.0;

// Predefined colors
const COLOR_RED     = new Color(255,   0,   0);
const COLOR_GREEN   = new Color(  0, 255,   0);
const COLOR_BLUE    = new Color(  0,   0, 255);

const COLOR_YELLOW  = new Color(255, 255,   0);
const COLOR_CYAN    = new Color(  0, 255, 255);
const COLOR_MAGENTA = new Color(255,   0, 255);

const COLOR_BLACK   = new Color(  0,   0,   0);
const COLOR_WHITE   = new Color(255, 255, 255);
